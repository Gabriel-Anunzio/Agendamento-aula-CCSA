# Agendamento de Aulas - Mackenzie

Sistema web para agendamento e visualização de horários de aulas, inspirado no tema Mackenzie.

## 🚀 Como Executar

1. Baixe os arquivos desta pasta.
2. Certifique-se de que os arquivos `ETAPA 1.csv` e `ETAPA 2.csv` estão na mesma pasta que o `index.html`.
3. Abra o arquivo `index.html` em qualquer navegador moderno.

## 📁 Estrutura de Arquivos

- `index.html`: Estrutura principal da aplicação.
- `app.js`: Lógica principal do calendário, troca de etapas e renderização dos eventos.
- `parse_csv.js`: Utilitário para ler e converter os arquivos CSV em dados utilizáveis.
- `extract_subjects.js`: Script auxiliar para extração de nomes de disciplinas.
- `ETAPA 1.csv` / `ETAPA 2.csv`: Dados das grades horárias.

## 🛠️ Tecnologias
- HTML5
- CSS3 (Vanilla)
- JavaScript (Vanilla)
- PapaParse (para leitura de CSV)
